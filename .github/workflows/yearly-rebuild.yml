name: Yearly rebuild

on:
  schedule:
    - cron: '0 6 1 1 *'  # 06:00 UTC on January 1st every year

jobs:
  trigger-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Vercel deploy hook
        run: curl -X POST "${{ secrets.VERCEL_DEPLOY_HOOK_URL }}"
